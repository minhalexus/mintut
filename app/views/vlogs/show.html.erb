<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @vlog.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @vlog.body %>
</p>

<%= link_to 'Edit', edit_vlog_path(@vlog) %> |
<%= link_to 'Back', vlogs_path %>
